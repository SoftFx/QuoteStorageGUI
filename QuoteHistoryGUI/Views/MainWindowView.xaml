<Window 
    Icon="../Icons/Storage32.ico"
    x:Class="QuoteHistoryGUI.Views.MainWindowView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:QuoteHistoryGUI.Views"
        xmlns:vs="clr-namespace:QuoteHistoryGUI"
        mc:Ignorable="d"
        ContentRendered ="Window_ContentRendered"
        Title="QuoteStorageGUI" Height="450" Width="625" Background="#F2F0ED" 
        HorizontalAlignment="Center"
        >

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="30"/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="70"/>
                <ColumnDefinition Width="148"/>

            </Grid.ColumnDefinitions>
            <Button Name="OpenButton" Content="Open" Grid.Column="0"  Width="60"  Margin="5" Command="{Binding OpenBtnClick}" Background="#F2F0ED" HorizontalAlignment="Left" BorderBrush="#F2F0ED" />
  
            <TextBlock Text="{Binding StoragePath}" Grid.Column="1"/>
        </Grid>

                <Border BorderThickness="2" Grid.Row="1">
            <Grid >
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="52"/>
                    <ColumnDefinition Width="148"/>
                    <ColumnDefinition MinWidth="30" />
                </Grid.ColumnDefinitions>
                <GroupBox Grid.Column="0" Header=" Storage Tree" Margin="2" Grid.ColumnSpan="2" >
                    <TreeView Name="treeView" TreeViewItem.Expanded="treeView_Expanded" ItemsSource="{Binding Path=Folders, Mode=TwoWay,NotifyOnSourceUpdated=True, UpdateSourceTrigger=PropertyChanged}" Margin="2" Background="White" VirtualizingStackPanel.IsVirtualizing="True" >
                        <TreeView.Resources>
                            <Style x:Key="ExpandingFolderStyle" TargetType="{x:Type Image}">
                                <Setter Property="Source" Value="../Icons/closeFolder.png"/>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding RelativeSource={RelativeSource AncestorType=TreeViewItem}, Path=IsExpanded}" Value="True">
                                        <Setter Property="Source" Value="../Icons/openFolder.png"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>

                            <DataTemplate DataType="{x:Type vs:LoadingFolder}">
                                <StackPanel Orientation="Horizontal" >
                                    <Image Source="../Icons/loading.png" Height="12" Width="12">
                                    </Image>
                                    <TextBlock Text="  " VerticalAlignment="Center"></TextBlock>
                                    <TextBlock Text="{Binding Path=Name}" VerticalAlignment="Center"></TextBlock>
                                </StackPanel>
                            </DataTemplate>
                            <HierarchicalDataTemplate ItemsSource="{Binding Path=Folders}" DataType="{x:Type vs:Folder}">
                                <StackPanel Orientation="Horizontal" >

                                    <Image Style="{StaticResource ExpandingFolderStyle}" Height="12" Width="12">
                                    </Image>
                                    <TextBlock Text="  " VerticalAlignment="Center"></TextBlock>
                                    <TextBlock Text="{Binding Path=Name}" VerticalAlignment="Center"></TextBlock>
                                    <TextBlock Text="  " VerticalAlignment="Center"></TextBlock>
                                </StackPanel>
                            </HierarchicalDataTemplate>



                        </TreeView.Resources>

                    </TreeView >
                </GroupBox>
                <GridSplitter Width="1" HorizontalAlignment="Right" VerticalAlignment="Stretch" ResizeBehavior="CurrentAndNext" Grid.Column="1"/>
                <GroupBox Grid.Column="2" Header="History" Margin="2" />
            </Grid>
        </Border>
    </Grid>
</Window>
